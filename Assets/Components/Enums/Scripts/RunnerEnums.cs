public enum SpeedState
{
    Stop,
    Walk,
    Run,
    Fly
}

public enum PlayerCommand
{
    Idle,
    MoveLeft,
    MoveRight,
    MoveUp,
    MoveDown,
    Jump
}

public enum CollectableType
{
    Fuel
}